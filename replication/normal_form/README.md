# Normal-Form / Matrix-Design Replication

Paper targets owned by this codebase:

- `fig_matrix_design_ablation`
- `fig_hidden_queries_ablation`
- `fig_memory_phase_ablation`
- `fig_continuous_follower_learning_ablation`
- `fig_reward_during_learning_ablation`

Matrix Design is runnable and expands to the four final-paper curves.  The
other four entries are deliberately marked `archived_data_only`: their plotted
legacy cohorts are in the journal reproducibility bundle, while the maintained
matrix package uses audited protocols that differ scientifically from those
historical runs.  See `replication/matrix_ablations/LEGACY_PROVENANCE.md`.

The removed Maintain/randomized target does not appear in the final paper.
Figure aggregation is maintained in the journal reproducibility bundle.
