# Normal-Form / Matrix-Design Replication

Paper targets owned by this codebase:

- `fig_maintain_randomized`
- `fig_matrix_design_ablation`

These are appendix experiments for randomized strategies and matrix-design
mechanism design.

TODO:

- Add plotting scripts that regenerate `simpleMatrixGame2_new` and
  `MatrixDesign-Ablation_new` from raw logs.
- Confirm whether all theorem-violation normal-form ablations remain companion
  `StackeRLberg` targets or should be ported here.
