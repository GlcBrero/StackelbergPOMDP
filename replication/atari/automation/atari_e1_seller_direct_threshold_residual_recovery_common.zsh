#!/bin/zsh

# v3 artifact profile over the shared residual-seller orchestration helpers.
# Sourcing this file never starts work and never admits v2 artifacts.

typeset -gx STACKPOMDP_E1_SELLER_RESIDUAL_PROFILE=v3-direct
source "${${(%):-%N}:A:h}/atari_e1_seller_threshold_residual_recovery_common.zsh"
