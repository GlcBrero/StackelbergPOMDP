# Companion-Code Replication TODO

Several paper appendix figures are currently owned by `Code/StackeRLberg`.
They are listed in `replication/targets.json` as `companion_stackerlberg`
targets:

- `fig_hidden_queries_ablation`
- `fig_memory_phase_ablation`
- `fig_reward_during_learning_ablation`
- `fig_continuous_follower_learning_ablation`

TODO:

- Add exact StackeRLberg experiment commands.
- Add output directories and plotting commands.
- Decide whether any of these should be ported into `StackelbergPOMDP` or stay
  as companion-code replication targets.
